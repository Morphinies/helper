import { ThemeConfig } from "antd";

const themeConfig: ThemeConfig = {
  token: {},
  components: {
    Typography: {
      titleMarginTop: 0,
      titleMarginBottom: 0,
    },
    Form: {},
  },
};

export default themeConfig;
